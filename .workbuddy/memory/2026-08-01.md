# 2026-08-01 工作日志

## 代码审查：node_pdf_to_md.py (两轮)

### 第一轮审查
- 发现主要问题：process 无返回值；节点逻辑不完整（仅上传PDF，缺下载解析结果/保存md步骤）；local_dir 创建后未使用；上传失败仅 logger.info 不抛异常；print(batch_id)；import requests 放函数内；MineruConfig token/url 缺 None 防御。
- 对照规范参考：base.py（NodeBase）、node_entry.py（正确写法有 return dict）。

### 第二轮审查（深度）
- 用户修改文件后复查，AST 分析确认 `process` 仍无 return 语句。
- 新增问题：
  - URL 硬编码 `https://mineru.net` 应改用 MineruConfig.mineru_base_url
  - 重复 import requests（函数内出现两次）
  - `batch_id = batch_id` 无意义自赋值
  - 用 raise Exception + except 做正常流程控制（state != 'done' 时 raise 然后 except 重试——是反模式，应直接 continue）
  - 无差别重试：401/403 鉴权错误也循环 300 秒
  - `data['extract_result'][0]` 无防御，空列表会 IndexError
  - except Exception 太宽，吞掉所有错误类型（甚至 KeyboardInterrupt）
  - `data_id: "abcd"` 硬编码占位值
  - zip_url 拿到后仍缺下载/解压/保存 md 的核心逻辑
